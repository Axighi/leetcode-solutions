mod solutions;

// use crate::solutions::roman_to_int::*;
// use crate::solutions::max_sub_array::*;

fn main() {
    // max_sub_array(vec![-2, 1, -3, 4, -1, 2, 1, -5, 4]);
    println!("{}", " ".as_bytes()[0]);
}
